name: books_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  bloc: ^8.1.2
  carousel_slider: ^4.2.1
  cupertino_icons: ^1.0.2
  dio: ^5.3.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_launcher_icons: ^0.13.1
  flutter_offline: ^3.0.0
  flutter_secure_storage: ^9.0.0
  fluttertoast: ^8.2.2
  get: ^4.6.6
  http: ^1.1.0
  image_picker: ^1.0.4
  modal_progress_hud_nsn: ^0.4.0
  pin_code_fields: ^8.0.1
  shimmer: ^3.0.0
  synchronized: ^3.1.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true
  image_path: "images/bookly.png"

flutter:
  uses-material-design: true
  assets:
    - images/
